import { useAuth } from '../lib/auth'
import { useMovementFilter } from '../lib/movement-filter'
export function SettingsPage() { const { session, clearSession } = useAuth(); const { filter, setFilter } = useMovementFilter(); return <section className="ledger-page"><header className="page-title"><div><p>Perfil</p><h1>Configurações</h1></div></header><div className="two-columns"><section className="ledger-panel"><h2>Conta</h2><p><strong>{session?.user.name}</strong><br />{session?.user.email}</p><button className="ledger-button" onClick={clearSession}>Sair da conta</button></section><section className="ledger-panel"><h2>Preferências</h2><p>Tema escuro Ledger está ativo.</p><fieldset><legend>Filtro padrão de movimentações</legend><label><input checked={filter === 'both'} name="filter" type="radio" onChange={() => setFilter('both')} />Todas</label><label><input checked={filter === 'income'} name="filter" type="radio" onChange={() => setFilter('income')} />Receitas</label><label><input checked={filter === 'expense'} name="filter" type="radio" onChange={() => setFilter('expense')} />Despesas</label></fieldset></section></div></section> }
