import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { CategorySpending, MonthlyEvolution } from '../types/api'
export function LedgerCharts({ categories, evolution }: { categories: CategorySpending[]; evolution: MonthlyEvolution[] }) { return <div className="ledger-chart-grid"><section className="ledger-panel"><h2>Gastos por categoria</h2>{categories.length === 0 ? <p className="ledger-empty">Nenhum gasto categorizado neste período.</p> : <><div aria-label="Gráfico de barras de gastos por categoria" className="ledger-chart"><ResponsiveContainer width="100%" height={260}><BarChart data={categories} layout="vertical" margin={{ left: 20 }}><CartesianGrid stroke="var(--ledger-border)" horizontal={false} /><XAxis type="number" /><YAxis dataKey="categoryName" type="category" width={100} /><Tooltip /><Bar dataKey="spent" fill="var(--accent-expense)" name="Gastos" radius={6} /></BarChart></ResponsiveContainer></div><table className="sr-only"><caption>Dados de gastos por categoria</caption><tbody>{categories.map((item) => <tr key={item.categoryId}><th>{item.categoryName}</th><td>{item.spent}</td></tr>)}</tbody></table></>}</section><section className="ledger-panel"><h2>Evolução dos últimos seis meses</h2>{evolution.length === 0 ? <p className="ledger-empty">Sem evolução mensal para exibir.</p> : <div aria-label="Gráfico de evolução mensal" className="ledger-chart"><ResponsiveContainer width="100%" height={260}><LineChart data={evolution}><CartesianGrid stroke="var(--ledger-border)" /><XAxis dataKey="month" /><YAxis /><Tooltip /><Legend /><Line dataKey="income" name="Receitas" stroke="var(--accent-income)" /><Line dataKey="expense" name="Despesas" stroke="var(--accent-expense)" /></LineChart></ResponsiveContainer></div>}</section></div> }
