import { BarChart3, Bot, FolderCog, Goal, Import, LayoutDashboard, Settings, WalletCards } from 'lucide-react'
import { useRef, useState } from 'react'
import { NavLink } from 'react-router-dom'
const items = [[LayoutDashboard, 'Visão geral', '/dashboard'], [WalletCards, 'Transações', '/transacoes'], [Import, 'Importar', '/importar'], [FolderCog, 'Categorias', '/categorias'], [Goal, 'Metas', '/metas'], [Bot, 'Assistente IA', '/assistant'], [Settings, 'Configurações', '/configuracoes']] as const
export function Sidebar({ collapsed }: { collapsed: boolean }) { const [tooltip, setTooltip] = useState<string | null>(null); const timer = useRef<ReturnType<typeof setTimeout> | null>(null); const clear = () => { if (timer.current) clearTimeout(timer.current); timer.current = null; setTooltip(null) }; const show = (label: string) => { if (collapsed) timer.current = setTimeout(() => setTooltip(label), 300) }; return <nav aria-label="Navegação principal" className={`ledger-sidebar ${collapsed ? 'ledger-sidebar--collapsed' : ''}`}>{!collapsed && <div className="ledger-brand"><BarChart3 aria-hidden /> <span>Ledger</span></div>}{items.map(([Icon, label, path]) => <span className="nav-item" key={path}><NavLink aria-describedby={tooltip === label ? `tooltip-${path.slice(1)}` : undefined} className="ledger-nav-link" to={path} onBlur={clear} onFocus={() => show(label)} onMouseEnter={() => show(label)} onMouseLeave={clear}><Icon aria-hidden size={19} /><span>{label}</span></NavLink>{tooltip === label && <span className="sidebar-tooltip" id={`tooltip-${path.slice(1)}`} role="tooltip">{label}</span>}</span>)}</nav> }
